language: node_js
sudo: true

matrix:
  include:
    - os: osx
      language: node_js
      node_js: "9"
      script:
        - npm i -g pkg
        - npm run build-darwin
cache:
  directories:
    - node_modules

branches:
  only:
    - master