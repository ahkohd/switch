const TextTemplate = {
    errorTitle: "Yikes 🤔",
    processNotFound: (name) => `It seems like ${name} is yet to be opened. Click to launch app! 🚀`,
    noHotApp: (key) => `No hot app as been assigned to NUM ${key}, add one at the Switch dock! 👆 `
}

export default TextTemplate;